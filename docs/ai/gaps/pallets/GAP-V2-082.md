---
id: GAP-V2-082
title: Tara por caja solo disponible en el modo de alta "promedio" — alta manual, masiva y por lector no permiten descontarla
module: pallets
category: domain-business
priority: P3
risk: low
size: M
status: ready
dependencies: []
target_files:
  - src/hooks/pallets/usePalletBoxCreation.ts
  - src/components/Admin/Pallets/PalletDialog/PalletView/index.tsx
  - src/components/Admin/Pallets/PalletDialog/MobilePalletView/AddManualScreen.tsx
created_at: 2026-07-05
updated_at: 2026-07-05
normalized_at: 2026-07-05
---

# GAP-V2-082 — Tara por caja solo disponible en el modo de alta "promedio"

## Problema

De los 5 métodos de alta de caja (`manual`, `promedio`/`average`, `masiva`/`bulk`, `lector`,
`códigos GS1`), únicamente el modo "promedio" (`usePalletBoxCreation.ts`, método `average`)
tiene un campo "Tara por Caja (kg)" que el sistema resta automáticamente del peso total antes de
calcular el neto por caja (`PalletView/index.tsx:1015-1030`).

En el modo "manual" (el más usado para altas caja a caja, tanto en desktop —
`PalletView/index.tsx:757-836` — como en mobile — `AddManualScreen.tsx` completo, que ni
siquiera tiene el concepto de tara en su formulario), el operario debe introducir directamente
el "Peso Neto (kg)" sin ninguna ayuda del sistema para descontar el peso conocido de la caja
vacía. Lo mismo ocurre en el modo "masiva" (lista de pesos, uno por línea).

Esto es coherente si el operario siempre pesa con báscula tarada o calcula el neto de cabeza,
pero introduce fricción operativa real y riesgo de error sistemático (olvidar restar la tara)
precisamente en el flujo que un operario de almacén usaría decenas de veces al día: dar de alta
cajas una a una a medida que las va pesando en báscula, donde la báscula normalmente muestra el
peso bruto (producto + caja).

## Objetivo

El operario puede, en el modo de alta caja a caja más usado (manual), introducir el peso tal
como lo lee en báscula (bruto) y dejar que el sistema calcule el neto restando una tara de caja
conocida — sin tener que cambiar de modo ni hacer el cálculo mentalmente.

## Contexto

Originalmente se planteó como complemento de GAP-V2-079 (fuente de la tara para calcular un
`grossWeight` real por caja). Jose decidió Opción A en GAP-V2-079 (eliminar el campo "Peso
bruto" por caja del historial, no calcularlo) — este GAP queda **independiente** de esa
decisión: la mejora de UX (reducir fricción al pesar en báscula) sigue siendo válida por sí
misma, ya no afecta a `grossWeight` en ningún sitio.

**Pregunta resuelta (Jose, 2026-07-05):** la tara de caja **varía caja a caja** — no es un
valor fijo por tipo de envase, por lo que no hay catálogo de tara por envase que prellenar.
Esto también resuelve implícitamente la pregunta sobre "recordar la última tara usada en la
sesión": no tendría sentido prellenar con el valor de una caja distinta, así que se mantiene el
patrón ya documentado de "sin memoria entre altas" (`saveDiscountPreferences`/
`resetBoxCreationDataPreservingDiscounts` en `palletHelpers.ts`) sin necesidad de excepción.

## Solución propuesta

- Añadir un campo opcional "Tara de caja (kg)" al modo manual (desktop y mobile), que si se
  rellena, calcula `netWeight = pesoIntroducido - taraCaja` antes de llamar a `addBox`.
- Mantener el campo actual como "Peso Neto (kg)" cuando la tara no se usa, para no romper el
  flujo de quien ya pesa neto directamente.
- Sin memoria de tara entre altas — el campo se introduce cada vez, consistente con el patrón
  ya establecido en el módulo.

## Criterios de aceptación

- [ ] El modo manual (desktop y mobile) permite introducir un peso bruto + tara de caja y el
      sistema calcula el neto automáticamente, sin romper el flujo existente de quien introduce
      el neto directamente.
- [ ] El campo de tara no persiste entre altas de caja (sin memoria de sesión) — coherente con
      el patrón ya documentado en `palletHelpers.ts`.

## Plan de validación

```text
npm run type-check
npm run lint
Verificación manual: dar de alta una caja en modo manual usando peso bruto + tara de caja y
confirmar que el peso neto resultante es correcto (bruto − tara).
```

## Notas de implementación

**Normalización (gap-normalizer, 2026-07-05):** marcado `blocked` por dos motivos:
(1) depende de la decisión de negocio de GAP-V2-079 (qué debe representar
`grossWeight` a nivel de caja), que está a su vez `blocked`; (2) el propio GAP
formula una "Pregunta para Jose" sin responder sobre si la tara de caja es fija
por tipo de envase o variable caja a caja — determina si la solución necesita un
catálogo de tara por envase o un campo manual repetido. No implementar hasta
tener ambas respuestas.

**Decisión de Jose (2026-07-05):** mantener como mejora de UX independiente de
GAP-V2-079 (se re-scopea, ver § Contexto — ya no depende de él). Tara variable
caja a caja, sin catálogo de envase ni memoria de sesión.

## Resultado

## Resultado de auditoría

## Links

- Auditoría de origen: `docs/ai/modules/pallets/audit.md`
- GAPs relacionados: GAP-V2-079 (peso bruto por caja fabricado en el historial)
