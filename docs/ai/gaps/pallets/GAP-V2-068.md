---
id: GAP-V2-068
title: Eliminar todas las cajas (desktop) no pide confirmación antes de ejecutar
module: pallets
category: ux-ui
priority: P0
risk: low
size: S
status: done
dependencies: []
target_files:
  - src/components/Admin/Pallets/PalletDialog/PalletView/index.tsx
created_at: 2026-07-05
updated_at: 2026-07-07
normalized_at: 2026-07-05
---

# GAP-V2-068 — Eliminar todas las cajas (desktop) no pide confirmación antes de ejecutar

## Problema

En la pestaña "Eliminar" de `PalletView` (desktop), el botón "Eliminar todas las cajas"
ejecuta la eliminación directamente al hacer clic, sin ningún diálogo de confirmación:

```tsx
// src/components/Admin/Pallets/PalletDialog/PalletView/index.tsx:2601-2609
<Button
  variant="destructive"
  className="w-full"
  onClick={() => handleOnClickDeleteAllBoxes()}
  disabled={isReadOnly}
>
  <Trash2 className="h-4 w-4" /> Eliminar todas las cajas
</Button>
```

`handleOnClickDeleteAllBoxes` (línea ~438-441) llama a `deleteAllBoxes()` sin ningún paso
intermedio. Esto viola directamente `design-context.md` § Modals ("Destructive confirmation
pattern": toda acción destructiva/irreversible debe pasar por `AlertDialog`) y la UX Principle
#1 documentada ("Destructive actions always require confirmation").

El equivalente mobile de la misma acción sí implementa la confirmación correctamente:

```tsx
// src/components/Admin/Pallets/PalletDialog/MobilePalletView/EliminarTab.tsx:87-115
<AlertDialog>
  <AlertDialogTrigger asChild>
    <Button size="lg" variant="destructive" className="h-14 w-full" disabled={boxes.length === 0}>
      <Trash2 className="mr-2 h-5 w-5" />
      Eliminar todas las cajas
    </Button>
  </AlertDialogTrigger>
  <AlertDialogContent>
    <AlertDialogHeader>
      <AlertDialogTitle>¿Eliminar todas las cajas?</AlertDialogTitle>
      <AlertDialogDescription>
        Se eliminarán las {boxes.length} caja{boxes.length !== 1 ? 's' : ''} del palet.
        Esta acción no se puede deshacer una vez guardado.
      </AlertDialogDescription>
    </AlertDialogHeader>
    ...
```

Es decir: la misma acción, sobre la misma entidad, tiene protección en mobile y no la
tiene en desktop — una divergencia de seguridad/UX entre las dos implementaciones del
mismo flujo.

## Objetivo

El botón "Eliminar todas las cajas" en desktop (`PalletView`) exige confirmación explícita
del usuario antes de ejecutar `deleteAllBoxes()`, con el mismo nivel de detalle que la
versión mobile (número de cajas afectadas, aviso de irreversibilidad).

## Contexto

`PalletView` (desktop) y `MobilePalletView` son implementaciones completamente separadas
del mismo flujo de edición de palet (ver nota de scope del audit). El resto de acciones
destructivas de `PalletView` sí usan `AlertDialog` correctamente (eliminar caja individual:
línea 2780-2804; borrar historial: línea 2807-2826) — solo "Eliminar todas las cajas" quedó
sin el patrón.

## Solución propuesta

Añadir un `AlertDialog` de confirmación para "Eliminar todas las cajas" en `PalletView`,
siguiendo el mismo patrón ya usado en el propio archivo para "eliminar caja" y "borrar
historial" (estado `deleteAllBoxesConfirmOpen`, `AlertDialogTrigger asChild` envolviendo el
botón, título y descripción con el número de cajas afectadas — replicar el copy de
`EliminarTab.tsx` en mobile para mantener consistencia de mensaje entre ambas superficies).

## Criterios de aceptación

- [ ] El botón "Eliminar todas las cajas" en desktop abre un `AlertDialog` antes de ejecutar
      `deleteAllBoxes()` — nunca se ejecuta en el `onClick` directo del botón.
- [ ] El `AlertDialogDescription` indica el número de cajas que se eliminarán, igual que en
      mobile (`EliminarTab.tsx`).
- [ ] `AlertDialogAction` usa el estilo destructivo ya establecido en el resto del archivo
      (`bg-destructive text-destructive-foreground hover:bg-destructive/90`).
- [ ] El comportamiento en `isReadOnly` se mantiene (botón deshabilitado, sin cambios).
- [ ] No se modifica ningún otro flujo de eliminación ya confirmado en el archivo.

## Plan de validación

```text
npm run type-check
npm run lint
Verificación manual: abrir un palet con cajas en desktop → pestaña Eliminar → clic en
"Eliminar todas las cajas" → confirmar que aparece el AlertDialog y que cancelar no borra
nada; confirmar que aceptar sí ejecuta el borrado.
```

## Notas de implementación

**Normalización (gap-normalizer, 2026-07-05):** confirmado `P0` tras ver el
conjunto completo de los 22 candidatos de esta pasada — es una acción destructiva
irreversible ejecutada sin ningún paso de confirmación en el flujo desktop
mientras el equivalente mobile sí lo protege; no compite en severidad con ningún
otro hallazgo `P0` salvo GAP-V2-078 (bug de dominio distinto, también confirmado
`P0`). Marcado `ready` — contenido completo y sin dependencias.

## Resultado

Añadido `AlertDialog` de confirmación para "Eliminar todas las cajas" en
`PalletView/index.tsx`, replicando el copy de `EliminarTab.tsx` mobile (número
de cajas afectadas, aviso de irreversibilidad). El botón ahora abre el diálogo
(`setDeleteAllBoxesConfirmOpen(true)`) en vez de ejecutar `deleteAllBoxes()`
directamente; la ejecución real ocurre en `handleConfirmDeleteAllBoxes`, ligada
al `AlertDialogAction`. `npm run type-check` y `npm run lint` limpios (sin
errores ni warnings nuevos).

**Corrección tras primera pasada de gap-auditor:** el conteo inicial usaba
`temporalPallet?.numberOfBoxes` (vía `getAvailableBoxesCount`, que excluye
cajas en producción), mientras que `deleteAllBoxes()` elimina el array
completo de cajas. Corregido a `temporalPallet?.boxes?.length` para que el
mensaje de confirmación cuente siempre el total real de cajas que se van a
eliminar.

## Revisión UX

```
UX REVIEW — FULL
════════════════
GAP: GAP-V2-068 — Eliminar todas las cajas (desktop) no pide confirmación antes de ejecutar
Reviewer: ux-reviewer agent
Mode: Full

FLOW SIMULATION SUMMARY
Steps simulated: 4 (abrir pestaña Eliminar → clic "Eliminar todas las cajas" → cancelar →
                    reabrir y confirmar)
User roles covered: cualquier rol con `canDeletePalletData` (no `isReadOnly`)
Edge cases covered: palet sin cajas, isReadOnly, cancelar vs confirmar, conteo de cajas

FLOW SIMULATION
───────────────
Role: usuario con permiso de eliminar datos de palet (canDeletePalletData), no read-only.
Entry point: PalletDialog (desktop) → pestaña "Eliminar" del editor de palet.

Step 1: Usuario abre la pestaña "Eliminar". Ve la card "Eliminar cajas" con input de código
        escaneado y el botón destructivo "Eliminar todas las cajas".
  → Action: clic en "Eliminar todas las cajas".
  → Result: se abre `AlertDialog` — título "¿Eliminar todas las cajas?", descripción con el
    conteo real de cajas (`temporalPallet?.boxes?.length`) y aviso de irreversibilidad.
  → Potential friction: ninguna — no se ejecuta nada hasta confirmar, exactamente el criterio
    de aceptación.

Step 2: Usuario pulsa "Cancelar".
  → Action: `AlertDialogCancel` cierra el diálogo vía `onOpenChange(false)`.
  → Result: `deleteAllBoxesConfirmOpen` vuelve a `false`, ninguna caja se elimina, estado
    intacto.
  → Potential friction: ninguna.

Step 3: Usuario reabre el diálogo y pulsa "Eliminar todas" (acción del `AlertDialogAction`).
  → Action: `handleConfirmDeleteAllBoxes` → `deleteAllBoxes()` + cierre del diálogo.
  → Result: `temporalPallet.boxes` pasa a `[]`, toast de éxito ("Todas las cajas eliminadas").
    El cambio es local (staged) — aún requiere "Guardar" para persistir, igual que el resto
    de operaciones de caja del mismo archivo (eliminar una caja, duplicar, editar). Copy
    coherente en todo el flujo: disparador, título y acción usan siempre el verbo "Eliminar".
  → Potential friction: ninguna relevante para el criterio de aceptación.

Edge cases simulated:
  → Empty state (palet sin cajas): el botón NO se deshabilita con 0 cajas (solo con
    `isReadOnly`), a diferencia del equivalente mobile (`EliminarTab.tsx`, que sí usa
    `disabled={boxes.length === 0}`). Si se pulsa con 0 cajas, se abre igualmente el
    `AlertDialog` con "Se eliminarán las 0 cajas del palet" — no rompe nada ni ejecuta una
    acción destructiva real, pero es una confirmación innecesaria para una operación sin
    efecto. No forma parte de los criterios de aceptación de este GAP (que solo pedía añadir
    la confirmación, sin tocar la condición de `disabled`) — se deja como observación, no
    bloqueante.
  → Permission edge (isReadOnly): el botón permanece deshabilitado, comportamiento sin
    cambios respecto al original — cumple el criterio de aceptación explícito.
  → Concurrent action: no aplica — la operación es 100% client-side sobre `temporalPallet`
    hasta pulsar "Guardar"; no hay llamada a API en este paso.
  → Mobile: fuera de scope — este GAP es exclusivamente desktop (`PalletView`), el
    equivalente mobile ya tenía el patrón correcto y no se modifica.

FINDINGS
✅ Working well: copy 100% consistente entre disparador/título/descripción/acción ("Eliminar"
   en las 4 superficies); conteo de cajas correcto y ya corregido a `boxes.length` (total real);
   estilo destructivo (`bg-destructive`) igual al resto del archivo; `isReadOnly` respetado sin
   regresión; no introduce loops ni un segundo diálogo de confirmación.
⚠️ Friction points: el botón no se deshabilita con 0 cajas (mobile sí lo hace) — permite abrir
   un diálogo de confirmación para una acción sin efecto real. Sugerencia de mejora futura
   (no bloqueante): `disabled={isReadOnly || (temporalPallet?.boxes?.length ?? 0) === 0}`.
❌ Blocking issues: ninguno.

UX PRINCIPLES CHECK
Destructive actions always require confirmation: ✅
Errors surface at the right level (n/a — no hay llamada a API en este paso): ✅
Density / chrome minimal, patrón AlertDialog estándar del proyecto: ✅
Icons Lucide-only (Trash2, ya usado en el resto del archivo): ✅

VERDICT: ✅ APPROVED

Score: 9/10
```

## Resultado de auditoría

**gap-auditor (2026-07-07):** done tras corrección del conteo de cajas
(`boxes.length` en vez de `numberOfBoxes`). `npm run type-check`/`lint`
limpios, diff aislado. **ux-reviewer (Full):** ✅ APPROVED (9/10) — copy
consistente, sin loops. Observación no bloqueante fuera de scope: el botón no
se deshabilita con 0 cajas (mobile sí lo hace).

## Links

- Auditoría de origen: `docs/ai/modules/pallets/audit.md`
- GAPs relacionados: GAP-V2-069 (mismo patrón de confirmación ausente en "Deshacer")
