# PesquerApp — Project Learnings (pointer)

> **Este archivo ya no contiene entradas propias.** Desde 2026-07-02, `.claude/project-learnings.md`
> (en la raíz del proyecto) es la **única fuente canónica** de memoria institucional —
> tanto para Claude Code como para Codex y cualquier otra herramienta que use este
> repositorio. Mantener dos copias del mismo contenido causó divergencia real (esta
> copia se quedó en 23 entradas mientras la canónica llegó a 32) sin que nadie las
> sincronizara.
>
> **Lee siempre `.claude/project-learnings.md` directamente** — no este archivo — para
> obtener las entradas actuales (AUDIT_RULE / CODEBASE_PATTERN / ANTI_PATTERN /
> CORRECTION).
>
> **Para escribir una entrada nueva** (desde Codex o cualquier otra herramienta):
> escríbela en `.claude/project-learnings.md`, siguiendo el mismo protocolo de
> siempre (proponer con evidencia a Jose, escribir solo tras confirmación explícita,
> comprobar duplicados/contradicciones primero). Jose ha autorizado explícitamente
> que Codex lea y escriba en `.claude/project-learnings.md` para este archivo en
> concreto — ver `docs/agent-system/rules/memory.md`.
>
> Este archivo se conserva como puntero (en vez de borrarse) porque varios workflows
> y skills del adaptador Codex (`docs/agent-system/workflows/*.md`,
> `.agents/skills/lapesquerapp-*/SKILL.md`) todavía referencian esta ruta — así no
> hace falta editar cada uno de ellos para que sigan encontrando la memoria real.
